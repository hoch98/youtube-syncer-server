#youtube-syncer server

Application for hosting the servers for the youtube-syncing extension

npm install
node main.js

Prompts whether it should ngrok forwarding or not (for hosting on internet), creates a config.json
Requires ngrok API key